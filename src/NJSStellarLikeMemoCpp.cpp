// AUTOGENERATED FILE - DO NOT MODIFY!
// This file generated by Djinni from stellar_like_wallet.djinni

#include "NJSStellarLikeMemoCpp.hpp"
#include "NJSObjectWrapper.hpp"
#include "NJSHexUtils.hpp"

using namespace v8;
using namespace node;
using namespace std;

NAN_METHOD(NJSStellarLikeMemo::getMemoType) {

    //Check if method called with right number of arguments
    if(info.Length() != 0)
    {
        return Nan::ThrowError("NJSStellarLikeMemo::getMemoType needs 0 arguments");
    }

    //Check if parameters have correct types

    //Unwrap current object and retrieve its Cpp Implementation
    auto cpp_impl = djinni::js::ObjectWrapper<ledger::core::api::StellarLikeMemo>::Unwrap(info.This());
    if(!cpp_impl)
    {
        return Nan::ThrowError("NJSStellarLikeMemo::getMemoType : implementation of StellarLikeMemo is not valid");
    }

    auto result = cpp_impl->getMemoType();

    //Wrap result in node object
    auto arg_0 = Nan::New<Integer>((int)result);

    //Return result
    info.GetReturnValue().Set(arg_0);
}
NAN_METHOD(NJSStellarLikeMemo::getMemoText) {

    //Check if method called with right number of arguments
    if(info.Length() != 0)
    {
        return Nan::ThrowError("NJSStellarLikeMemo::getMemoText needs 0 arguments");
    }

    //Check if parameters have correct types

    //Unwrap current object and retrieve its Cpp Implementation
    auto cpp_impl = djinni::js::ObjectWrapper<ledger::core::api::StellarLikeMemo>::Unwrap(info.This());
    if(!cpp_impl)
    {
        return Nan::ThrowError("NJSStellarLikeMemo::getMemoText : implementation of StellarLikeMemo is not valid");
    }

    auto result = cpp_impl->getMemoText();

    //Wrap result in node object
    auto arg_0 = Nan::New<String>(result).ToLocalChecked();

    //Return result
    info.GetReturnValue().Set(arg_0);
}
NAN_METHOD(NJSStellarLikeMemo::getMemoId) {

    //Check if method called with right number of arguments
    if(info.Length() != 0)
    {
        return Nan::ThrowError("NJSStellarLikeMemo::getMemoId needs 0 arguments");
    }

    //Check if parameters have correct types

    //Unwrap current object and retrieve its Cpp Implementation
    auto cpp_impl = djinni::js::ObjectWrapper<ledger::core::api::StellarLikeMemo>::Unwrap(info.This());
    if(!cpp_impl)
    {
        return Nan::ThrowError("NJSStellarLikeMemo::getMemoId : implementation of StellarLikeMemo is not valid");
    }

    auto result = cpp_impl->getMemoId();

    //Wrap result in node object
    auto arg_0 = NJSBigInt::wrap(result);


    //Return result
    info.GetReturnValue().Set(arg_0);
}
NAN_METHOD(NJSStellarLikeMemo::getMemoHash) {

    //Check if method called with right number of arguments
    if(info.Length() != 0)
    {
        return Nan::ThrowError("NJSStellarLikeMemo::getMemoHash needs 0 arguments");
    }

    //Check if parameters have correct types

    //Unwrap current object and retrieve its Cpp Implementation
    auto cpp_impl = djinni::js::ObjectWrapper<ledger::core::api::StellarLikeMemo>::Unwrap(info.This());
    if(!cpp_impl)
    {
        return Nan::ThrowError("NJSStellarLikeMemo::getMemoHash : implementation of StellarLikeMemo is not valid");
    }

    auto result = cpp_impl->getMemoHash();

    //Wrap result in node object
    auto arg_0 = Nan::New<String>("0x" + djinni::js::hex::toString(result)).ToLocalChecked();


    //Return result
    info.GetReturnValue().Set(arg_0);
}
NAN_METHOD(NJSStellarLikeMemo::getMemoReturn) {

    //Check if method called with right number of arguments
    if(info.Length() != 0)
    {
        return Nan::ThrowError("NJSStellarLikeMemo::getMemoReturn needs 0 arguments");
    }

    //Check if parameters have correct types

    //Unwrap current object and retrieve its Cpp Implementation
    auto cpp_impl = djinni::js::ObjectWrapper<ledger::core::api::StellarLikeMemo>::Unwrap(info.This());
    if(!cpp_impl)
    {
        return Nan::ThrowError("NJSStellarLikeMemo::getMemoReturn : implementation of StellarLikeMemo is not valid");
    }

    auto result = cpp_impl->getMemoReturn();

    //Wrap result in node object
    auto arg_0 = Nan::New<String>("0x" + djinni::js::hex::toString(result)).ToLocalChecked();


    //Return result
    info.GetReturnValue().Set(arg_0);
}
NAN_METHOD(NJSStellarLikeMemo::memoValuetoString) {

    //Check if method called with right number of arguments
    if(info.Length() != 0)
    {
        return Nan::ThrowError("NJSStellarLikeMemo::memoValuetoString needs 0 arguments");
    }

    //Check if parameters have correct types

    //Unwrap current object and retrieve its Cpp Implementation
    auto cpp_impl = djinni::js::ObjectWrapper<ledger::core::api::StellarLikeMemo>::Unwrap(info.This());
    if(!cpp_impl)
    {
        return Nan::ThrowError("NJSStellarLikeMemo::memoValuetoString : implementation of StellarLikeMemo is not valid");
    }

    auto result = cpp_impl->memoValuetoString();

    //Wrap result in node object
    auto arg_0 = Nan::New<String>(result).ToLocalChecked();

    //Return result
    info.GetReturnValue().Set(arg_0);
}

NAN_METHOD(NJSStellarLikeMemo::New) {
    //Only new allowed
    if(!info.IsConstructCall())
    {
        return Nan::ThrowError("NJSStellarLikeMemo function can only be called as constructor (use New)");
    }
    info.GetReturnValue().Set(info.This());
}


Nan::Persistent<ObjectTemplate> NJSStellarLikeMemo::StellarLikeMemo_prototype;

Local<Object> NJSStellarLikeMemo::wrap(const std::shared_ptr<ledger::core::api::StellarLikeMemo> &object) {
    Nan::EscapableHandleScope scope;
    Local<ObjectTemplate> local_prototype = Nan::New(StellarLikeMemo_prototype);

    Local<Object> obj;
    if(!local_prototype.IsEmpty())
    {
        obj = local_prototype->NewInstance(Nan::GetCurrentContext()).ToLocalChecked();
        djinni::js::ObjectWrapper<ledger::core::api::StellarLikeMemo>::Wrap(object, obj);
    }
    else
    {
        Nan::ThrowError("NJSStellarLikeMemo::wrap: object template not valid");
    }
    return scope.Escape(obj);
}

NAN_METHOD(NJSStellarLikeMemo::isNull) {
    auto cpp_implementation = djinni::js::ObjectWrapper<ledger::core::api::StellarLikeMemo>::Unwrap(info.This());
    auto isNull = !cpp_implementation ? true : false;
    return info.GetReturnValue().Set(Nan::New<Boolean>(isNull));
}

void NJSStellarLikeMemo::Initialize(Local<Object> target) {
    Nan::HandleScope scope;

    Local<FunctionTemplate> func_template = Nan::New<FunctionTemplate>(NJSStellarLikeMemo::New);
    Local<ObjectTemplate> objectTemplate = func_template->InstanceTemplate();
    objectTemplate->SetInternalFieldCount(1);

    func_template->SetClassName(Nan::New<String>("NJSStellarLikeMemo").ToLocalChecked());

    //SetPrototypeMethod all methods
    Nan::SetPrototypeMethod(func_template,"getMemoType", getMemoType);
    Nan::SetPrototypeMethod(func_template,"getMemoText", getMemoText);
    Nan::SetPrototypeMethod(func_template,"getMemoId", getMemoId);
    Nan::SetPrototypeMethod(func_template,"getMemoHash", getMemoHash);
    Nan::SetPrototypeMethod(func_template,"getMemoReturn", getMemoReturn);
    Nan::SetPrototypeMethod(func_template,"memoValuetoString", memoValuetoString);
    Nan::SetPrototypeMethod(func_template,"isNull", isNull);
    //Set object prototype
    StellarLikeMemo_prototype.Reset(objectTemplate);

    //Add template to target
    Nan::Set(target, Nan::New<String>("NJSStellarLikeMemo").ToLocalChecked(), Nan::GetFunction(func_template).ToLocalChecked());
}
